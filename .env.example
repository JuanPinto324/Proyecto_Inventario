# Copia este archivo como .env y llénalo con tus valores
SECRET_KEY=cambia-esto-por-una-clave-segura-larga
# En desarrollo deja DATABASE_URL vacío para usar SQLite
# En Render, esto se configura automáticamente con PostgreSQL
# DATABASE_URL=postgresql://user:pass@host/dbname
